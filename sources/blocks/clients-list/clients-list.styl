.clients-list {
  .row {
    justify-content center
    margin-left rem(-32.5)
    margin-right rem(-32.5)
  }
  .client-item {
    flex none
    width uq('calc(100% - ' + rem(65) + ')')
    max-width rem(196)
    margin 0 rem(32.5) rem(55)
  }

  @media (min-width em-root(480)) {
    .client-item {
      //width uq('calc(' + percentage(1/2) + ' - ' + rem(30) + ')')
    }
  }

  @media (min-width $screen-sm-min) {
    .client-item {
      //width uq('calc(' + percentage(1/3) + ' - ' + rem(30) + ')')
    }
  }

  @media (min-width $screen-md-min) {
    .client-item {
      //width uq('calc(' + percentage(1/4) + ' - ' + rem(30) + ')')
    }
  }
}